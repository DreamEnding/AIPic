# AIPic 图像工作室

以内容为中心的原生桌面工具风格。照片拥有最高视觉优先级，工具与参数是安静、稳定的辅助界面。

## 字体与层次

使用设备系统字体：Apple 设备使用系统 San Francisco 与苹方，Windows 回退到 Segoe UI 与微软雅黑。不加载远程字体。正文与输入保持清晰，数字计数使用等宽数字；标题依靠字重和留白建立层级，避免全屏粗体。

## 表面与颜色

浅色以 `#f5f5f7` 为背景、白色为工作面、`#1d1d1f` 为正文。深色随系统偏好切换，使用深灰分层表面。蓝色仅强调当前选择、焦点与主要动作。错误、成功和处理中同时提供文字或图形提示。

颜色、边框、阴影、圆角和动效由 `src/index.css` 的语义变量统一管理。使用细分隔线区分区域；照片画布保持中性，避免彩色渐变与网格干扰判断。

## 布局

修图工作台直接填充页面，移除全局与工作室标题横栏。右侧创作设置面板顶部提供设置入口；AI Native 位于工具首位并标记 NEW，默认展示纸刊海报。左侧选择工具，中央预览，右侧编辑提示词与输出参数。空间不足时分区自然换行，不缩小所有控件来挤入桌面布局。

## 控件与动效

统一细线图标、分段选择器、圆角输入框和克制的浮层。触控目标至少 44px，保留可见的键盘焦点。使用短暂的颜色、透明度和轻微位移反馈，不使用持续闪烁和装饰性循环。遵循 `prefers-reduced-motion`，保留浏览器缩放。

## 实现边界

此次改版调整表现层、工具排列与设置入口，不改变生图后端协议、图片存储或 API 密钥使用方式。测试与浏览器自动化按用户要求不执行；生产构建用于更新本地页面。
